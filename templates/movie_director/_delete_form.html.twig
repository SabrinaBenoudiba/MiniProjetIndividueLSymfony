<form method="post" action="{{ path('app_movie_director_delete', {'id': movie_director.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ movie_director.id) }}">
    <button class="btn">Delete</button>
</form>
