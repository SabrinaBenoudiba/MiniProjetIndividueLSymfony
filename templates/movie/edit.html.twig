{% extends 'base.html.twig' %}

{% block title %}Mise à jour du film{% endblock %}

{% block body %}

<div class="container">
    <h1 class="title text-center">Mise à jour du film</h1>

        {{ include('movie/new.html.twig', {'button_label': 'Update'}) }}

        <a class="btn btn-outline-primary" href="{{ path('app_movie_show') }}">retour à la liste</a>

        {# <a class= "btn btn-outline-danger">{{ include('product/_delete_form.html.twig') }}</a> #}
</div>
    
{% endblock %}
